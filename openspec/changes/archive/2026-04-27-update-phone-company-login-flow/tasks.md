## 1. API 与类型契约调整
- [x] 1.1 定义并接入认证接口契约：`/api/auth/login`、`/api/auth/logout`、`/api/auth/me`、`/api/auth/companies`。
- [x] 1.2 更新登录请求/响应与当前用户响应类型，包含 `phone/companyCode/role/position/mustChangePassword`。
- [x] 1.3 保留 `refreshToken` 字段但停用自动刷新调用路径。

## 2. 登录页面流程重构
- [x] 2.1 将登录默认且唯一入口调整为手机号登录，隐藏其他登录相关入口。
- [x] 2.2 实现手机号校验后查询公司列表，并支持公司选择。
- [x] 2.3 在公司已选择前禁用密码登录提交；提交时按新接口字段发送。
- [x] 2.4 处理登录成功、失败、公司列表为空、手机号非法等用户反馈。

## 3. 用户信息与权限接入
- [x] 3.1 使用 `/api/auth/me` 作为登录后用户信息来源。
- [x] 3.2 新增独立角色映射配置文件：`role/position -> frontend roles`。
- [x] 3.3 固定使用前端静态路由做菜单与访问控制，并保留独立菜单权限配置能力。

## 4. 验证与回归
- [x] 4.1 更新/新增登录相关单测（至少覆盖手机号校验、公司查询、登录提交流程）。
- [x] 4.2 运行 `pnpm lint`、`pnpm typecheck`、`pnpm test` 并修复本变更引入问题。
- [x] 4.3 更新相关文档（登录流程、权限来源、接口路径）并完成自检。
